import { Text } from 'react-native';
import { app, db, getFirestore, collection, addDoc } from '../firebase/index';

const deleteHabitBtn = async () => {

}

export default function TimeFormat() {
  return (
    <Text>This is the DeleteHabit component.</Text>
  )
}